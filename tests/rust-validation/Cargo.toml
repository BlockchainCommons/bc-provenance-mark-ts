[package]
name = "provenance-mark-validation"
version = "0.1.0"
edition = "2021"
publish = false

# The reference and its siblings, pinned to the released crates the port
# tracks. Nothing is patched.
[dependencies]
provenance-mark = "=0.24.0"
bc-envelope = "=0.43.0"
bc-ur = "=0.19.2"
dcbor = "=0.25.2"
known-values = "=0.15.5"
url = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
